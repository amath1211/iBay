<%= form_for(all_item) do |f| %>
  <% if all_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(all_item.errors.count, "error") %> prohibited this all_item from being saved:</h2>

      <ul>
      <% all_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
